/**
 * The fourteen crafted accounts, now owned by `src/form-cases.ts`.
 *
 * They ship as `@kodama/engine/form-cases` so the `/styles` page can draw a
 * specimen per style - only four of the fourteen forms are reachable from the ten
 * real fixtures, so a page built on those would document four styles and guess at
 * ten. This file stays so the suites and `gate-4.ts` keep their import path.
 */

export * from "../../src/form-cases.js";
